<div id='post_content'>
  <% @posts.each do |post| %>
    <h2 class='title'><%= link_to post.title, post %></h2>
    <p class='body'><%= truncate(post.body, length:125) %></p>
    <p class='date'><%= time_ago_in_words(post.created_at) %> ago</p>
  <% end %>
</div>